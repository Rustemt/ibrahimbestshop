SELECT 
C.FIRMAADI,
B.IND,
'Alýnan Sipariþ' As BELGETIPI,B.TUTAR,B.PARABIRIMI,B.BELGENO,B.TARIH
FROM [FIRMADONEM]TBLALSIPBASLIK AS B 
LEFT JOIN [FIRMAID]TBLCARI AS C ON (C.IND = B.FIRMANO) 
WHERE B.IPTAL =0 AND BELGETIPI IN (60) and (C.IND=[FIRMACARIKODU])
